const http = require('http'); const endpoints = ['/api/v1/admin/categories', '/api/v1/admin/products', '/api/v1/admin/brands', '/api/v1/admin/orders']; async function test() { for (const ep of endpoints) { const start = Date.now(); await new Promise(r => http.get('http://localhost:5000' + ep, res => { res.on('data', ()=>{}); res.on('end', () => r()); })); console.log(ep, Date.now() - start, 'ms'); } } test();
